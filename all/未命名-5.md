```mermaid
graph LR
%% 业务模块子图
subgraph "用户模块"
A[注册] --> B(用户认证)
C[登录] --> B
D[个人中心] --> B
D --> E[查看私信]
D --> F[发布素材]
D --> G[查看发布记录]
D --> H[处理私信]
D --> I["关注/粉丝"]
end

subgraph "素材管理模块"
J[上传素材] --> K(素材信息填写)
J --> L{待审核}
M[编辑素材] --> N[用户删除自有素材]
M --> O[管理员修改素材分类]
M --> P[管理员删除违规素材]
M --> Q[管理员新增专题素材]
M --> R[管理员修改专题素材]
L --> S(管理员审核)
end

subgraph "交互模块"
T[评论管理] --> U(查看评论)
T --> V(提交评论)
T --> W(删除评论)
X[素材下载] --> Y(获取下载链接)
Z[搜索功能] --> AA(关键词搜索)
Z --> AB(搜索历史)
AC[素材上传交互] --> AD(创建素材)
end

subgraph "管理员模块"
AE[素材管理] --> AF(素材列表管理)
AE --> AG(素材审核)
AH[分类管理] --> AI("添加/编辑/删除分类")
AJ[专题管理] --> AK("添加/编辑/删除专题")
AK --> AL(管理专题内素材)
AM[用户管理] --> AN("编辑/删除用户")
AM --> AO(角色设置)
AM --> AP(积分管理)
AQ[系统功能] --> AR(控制台首页)
AQ --> AS(管理员登录)
end

%% 支撑架构子图
subgraph "支撑架构层"
DB[("数据库MySQL")]
Cache[("Redis缓存")]
Service[("核心服务层")]
FS[("MinIO文件存储")]
Msg[("消息模块")]
end

%% 核心业务关联（无重复、无闭环、逻辑顺畅）
B --> D %% 用户认证指向个人中心
AS --> B %% 管理员登录走统一用户认证
S --> J %% 审核结果回流素材上传流程
S --> M %% 审核结果关联素材编辑流程

%% 业务模块统一指向支撑层（无重复拉线）
用户模块 --> DB
用户模块 --> Service
用户模块 --> Cache

素材管理模块 --> DB
素材管理模块 --> Service
素材管理模块 --> Cache
素材管理模块 --> FS

交互模块 --> DB
交互模块 --> Service
交互模块 --> Cache
交互模块 --> FS
交互模块 --> Msg

管理员模块 --> DB
管理员模块 --> Service
管理员模块 --> Cache

%% 内部子功能归属关联（无反向重复）
D --> E & F & G & H & I
T --> U & V & W
Z --> AA & AB
AE --> AF & AG
AH --> AI
AJ --> AK & AL
AM --> AN & AO & AP
AQ --> AR & AS
J --> K & L
M --> N & O & P & Q & R

```