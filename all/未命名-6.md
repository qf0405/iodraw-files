```mermaid
flowchart TD
    A[开始] --> B[后端校验JWT Token]
    B --> C{是否已登录?}
    C -->|未登录| D[拦截上传请求]
    C -->|已登录| E[解析Token获取用户角色]
    E --> F{是否为管理员?}
    F -->|是| G[仅允许上传专题类型素材]
    F -->|否| H[允许上传普通素材]
    G & H --> I[接收素材文件与基础信息<br/>(名称/分类/标签/描述)]
    I --> J{文件校验<br/>(大小≤500MB、格式合法)?}
    J -->|不合法| K[返回错误信息，终止上传]
    J -->|合法| L[调用MinIO SDK存储文件]
    L --> M[按素材类型存入对应存储桶]
    M --> N[获取文件访问URL]
    N --> O[存入素材数据表<br/>状态设为「待审核」]
    O --> P[同步素材状态至ES]
    P --> Q[上传完成，等待管理员审核]
    Q --> R[结束]
```